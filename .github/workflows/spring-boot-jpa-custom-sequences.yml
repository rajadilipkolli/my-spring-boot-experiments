name: spring-boot-jpa-customsequence

on:
  push:
    paths:
      - "jpa/spring-boot-jpa-customsequence/**"
    branches: [main]
  pull_request:
    paths:
      - "jpa/spring-boot-jpa-customsequence/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    name: Run Unit & Integration Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "jpa/spring-boot-jpa-customsequence"
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Shallow clones should be disabled for a better relevancy of analysis

      - name: Set up JDK 17
        uses: actions/setup-java@v3.11.0
        with:
          java-version: 17
          distribution: "zulu"
          cache: "maven"
      - name: Build and analyze
        run: ./mvnw clean verify
      - name: Codecov
        uses: codecov/codecov-action@v3.1.2

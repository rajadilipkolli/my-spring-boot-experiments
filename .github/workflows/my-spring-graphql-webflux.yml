name: my-spring-graphql-webflux

on:
  push:
    paths:
      - "my-spring-graphql-webflux/**"
    branches: [main]
  pull_request:
    paths:
      - "my-spring-graphql-webflux/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    name: Run Unit & Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Shallow clones should be disabled for a better relevancy of analysis

      - name: Set up JDK 18
        uses: actions/setup-java@v3.4.0
        with:
          java-version: 18
          distribution: "zulu"
          cache: "gradle"

      - name: Build and analyze
        run: |
          cd my-spring-graphql-webflux
          chmod +x gradlew
          ./gradlew clean build --info

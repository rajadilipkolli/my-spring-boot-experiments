name: my-spring-hibernate2ndlevelcache

on:
  push:
    paths:
      - "my-spring-hibernate2ndlevelcache/**"
    branches: [main]
  pull_request:
    paths:
      - "my-spring-hibernate2ndlevelcache/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    name: Run Unit & Integration Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: my-spring-hibernate2ndlevelcache
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Shallow clones should be disabled for a better relevancy of analysis

      - name: Set up JDK 18
        uses: actions/setup-java@v3.5.0
        with:
          java-version: 18
          distribution: "zulu"
          cache: "maven"

      - name: Build and analyze
        run: ./mvnw clean verify package

mutation addTagsToPost($addTagRequest: AddTagRequest!) {
    addTagsToPost(addTagRequest: $addTagRequest) {
        id
        title
        content
        createdOn
        comments {
            id
            review
            createdOn
        }
        tags {
            id
            name
        }
    }
}
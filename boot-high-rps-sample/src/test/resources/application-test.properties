# Test properties for integration/CI
app.kafka.events-topic.tombstone-retention-ms=3000
# Enable blocking wait-for-tombstone behavior during tests so interactive queries see tombstones quickly
app.wait-for-tombstone=true
